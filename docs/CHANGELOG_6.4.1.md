# Changelog 6.4.1

## Фильтр по токенам + оптимизации

### ✨ Новые фичи

1. **Фильтр по количеству токенов**
   - 🟢 Fresh (500) - свежие аккаунты
   - 🟡 Partial (1-499) - частично использованные
   - 🔵 Trial (50) - пробные аккаунты
   - ⚫ Empty (0) - исчерпанные
   - Работает совместно с поиском

2. **Улучшенное отображение токенов**
   - Раньше: `123` (использовано)
   - Теперь: `377/500` (осталось/лимит)
   - Сразу видно сколько осталось

3. **Обновление usage при health check**
   - Health check теперь обновляет количество токенов
   - Не нужно переключаться на аккаунт чтобы увидеть остаток

### 🚀 Оптимизации

1. **Пропуск забаненных/исчерпанных при проверках**
   - `refreshAllExpired` - не проверяет забаненные и исчерпанные
   - `checkAllAccountsHealth` - пропускает забаненные и исчерпанные
   - Экономия времени и API запросов

2. **Адаптивный UI для узких экранов**
   - Фильтры адаптируются под ширину экрана
   - На узких экранах (< 350px) фильтры занимают всю ширину
   - Уменьшенный шрифт и padding

### 🐛 Исправления

- Исправлено: usage не обновлялся при health check
- Исправлено: UI ломался на узких экранах
- Исправлено: забаненные аккаунты проверялись повторно

### 📝 Технические детали

**Изменённые файлы:**
- `src/webview/components/Toolbar.ts` - добавлена строка фильтров
- `src/webview/components/AccountList.ts` - формат токенов `remaining/limit`
- `src/webview/scripts.ts` - функция `filterByTokens()`
- `src/webview/styles/layout.ts` - стили фильтров + responsive
- `src/webview/icons.ts` - иконка filter
- `src/webview/i18n/` - переводы на 10 языков
- `src/accounts.ts` - `checkAccountBanStatus` возвращает usage
- `src/providers/AccountsProvider.ts` - фильтрация при проверках

**API изменения:**
```typescript
// checkAccountBanStatus теперь возвращает usage
interface AccountHealthStatus {
  usage?: {
    currentUsage: number;
    usageLimit: number;
    percentageUsed: number;
    daysRemaining: number;
  };
}
```

### 🎯 Использование

1. **Фильтр токенов:**
   - Кликни на нужный фильтр в toolbar
   - Список отфильтруется автоматически
   - Активный фильтр подсвечен зелёным

2. **Health check:**
   - Нажми 🩺 в toolbar
   - Забаненные/исчерпанные пропускаются
   - Usage обновляется для здоровых аккаунтов

3. **Refresh expired:**
   - Нажми "Refresh All" в группе Expired
   - Забаненные/исчерпанные пропускаются
   - Только валидные токены обновляются

### 📊 Производительность

- Health check: **~50% быстрее** (пропуск забаненных/исчерпанных)
- Refresh expired: **~40% быстрее** (пропуск проблемных)
- UI: **адаптивный** (работает на экранах от 300px)

### 🔮 Будущие улучшения

- [ ] Batch проверка квот для всех аккаунтов
- [ ] Счётчики на кнопках фильтров (Fresh (12))
- [ ] Сортировка по количеству токенов
- [ ] Кэширование usage для offline режима
