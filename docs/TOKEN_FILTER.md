# Token Filter Feature

## Описание

Фильтр по количеству оставшихся токенов в аккаунтах AWS Builder ID.

## Категории фильтров

| Фильтр | Описание | Остаток токенов |
|--------|----------|-----------------|
| 🟢 **Fresh** | Свежие аккаунты | 500 (не использованные) |
| 🟡 **Partial** | Частично использованные | 1-499 (кроме 50) |
| 🔵 **Trial** | Trial аккаунты | 50 |
| ⚫ **Empty** | Исчерпанные | 0 |

## Использование

1. В toolbar появилась вторая строка с фильтрами
2. Кликни на нужный фильтр (Fresh/Partial/Trial/Empty)
3. Список аккаунтов отфильтруется автоматически
4. Фильтр работает совместно с поиском

## Отображение токенов

Теперь в списке аккаунтов показывается:
- **Раньше**: `123` (использовано)
- **Теперь**: `377/500` (осталось/лимит)

## Технические детали

### Файлы изменены:
- `src/webview/components/Toolbar.ts` - добавлена строка фильтров
- `src/webview/components/AccountList.ts` - изменён формат отображения токенов
- `src/webview/scripts.ts` - добавлена функция `filterByTokens()`
- `src/webview/styles/layout.ts` - стили для фильтров
- `src/webview/icons.ts` - добавлена иконка filter
- `src/webview/i18n/` - переводы на 10 языков

### Логика фильтрации:

```typescript
function filterByTokens(filter) {
  switch (filter) {
    case 'fresh':   // tokens === 500
    case 'partial': // tokens > 0 && tokens < 500 && tokens !== 50
    case 'trial':   // tokens === 50
    case 'empty':   // tokens === 0
  }
}
```

## Будущие улучшения

- [ ] Batch проверка квот для всех аккаунтов (сейчас только для активного)
- [ ] Кэширование остатков токенов в usage cache
- [ ] Сортировка по количеству токенов
- [ ] Показывать количество аккаунтов в каждой категории фильтра
